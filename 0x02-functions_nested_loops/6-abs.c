#include "main.h"

/**
 * _abs - Prototype for the absolute integer
 * @n: return value
 *
 * Return: integer of the absolute value
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
